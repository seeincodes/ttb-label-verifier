# TTB Label Verification — pinned runtime dependencies.
# Versions track docs/TECH_STACK.md. Use `make install` to install.

# Web framework + server
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# Templating + form handling
jinja2>=3.1,<4.0
python-multipart>=0.0.9,<1.0

# Schemas + config
pydantic>=2.7,<3.0
pydantic-settings>=2.4,<3.0

# Vision model SDKs
google-generativeai>=0.8
openai>=1.40

# HTTP client + fuzzy matching + caching
httpx>=0.27,<1.0
rapidfuzz>=3.9,<4.0
cachetools>=5.4,<6.0

# Testing (used by `make test` and `make eval`)
pytest>=8.3,<9.0
